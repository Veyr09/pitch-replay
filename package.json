{
  "name": "pitch-replay",
  "version": "0.1.0",
  "private": true,
  "description": "A 2D football match replay engine: sparse event log in, continuous 90-minute animation out.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --target=es2020 --minify --outfile=docs/bundle.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "pixi.js": "^8.5.0"
  }
}
